
> **Context:** We are building a Beatbox-to-MIDI Transient Slicer MVP. The core mechanic relies on a C++ WASM DSP engine processing audio buffers and returning hitpoint data to a React frontend. 
> 
> **Current Issue:** The parameter sliders in `Controls.tsx` (Sensitivity, Noise Floor, Refractory) are disconnected from the `useHitManager.ts` update loop. Changing sliders does not trigger a recalculation in the WASM engine, nor does it update the Canvas.
> 
> **Your Task:**
> 1. Trace the data flow starting from the `onChange` or `onValueChange` events in `Controls.tsx`.
> 2. Check how these values are passed to `useHitManager.ts`. 
> 3. Verify if `useHitManager.ts` is successfully calling the `AudioProcessor.js` methods to update the WASM engine. 
> 4. Identify the exact point of failure (e.g., missing dependency arrays in `useEffect`, dropped props, or broken method bindings). 
> 
> Do not write any fix yet. Output a brief, bulleted diagnostic report detailing exactly where the React state $\rightarrow$ WASM communication chain is broken.

