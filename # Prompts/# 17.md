

### **MASTER TDD EXECUTION PROMPT: STRICT MOCKUP ALIGNMENT (FONTS, LINES, HEIGHTS)**

**Role & Instructions for Agent:**
You are an expert Frontend and Canvas UI engineer. Your objective is to style the application to perfectly match the provided mockup. Execute the following three phases strictly in order. Do not skip ahead. After completing the modifications for a phase, pause, build the app, and wait for the user to visually verify the checkpoint.

---

### **PHASE 1: Global Font Replacement (Google Sans Flex)**
**Objective:** Replace all existing fonts (including monospace numbers) with Google Sans Flex to match the mockup's typography.

1.  **`index.html` & `index.css` (Font Import & Global Assignment):**
    * Import **Google Sans Flex**. (If fetching from a CDN is required, inject the appropriate `@import` or `<link>`).
    * Apply it globally to the `body` in `index.css`:
      ```css
      body {
        font-family: 'Google Sans Flex', sans-serif !important;
        -webkit-font-smoothing: antialiased;
        text-rendering: optimizeLegibility;
      }
      ```
2.  **`tailwind.config.js`:**
    * Override the default Tailwind sans family to prioritize `'Google Sans Flex'`.
3.  **Component Sweep (Remove Monospace):**
    * Search `App.tsx`, `Transport.tsx`, and all UI components. Remove **every** instance of the `font-mono` class. 
    * For the timecode and tempo displays, replace `font-mono` with `tabular-nums` so the Google Sans Flex numbers align perfectly without jittering during playback.
4.  **Canvas Contexts (`RulerCanvas.tsx`, `SlicerCanvas.tsx`, `TimeSigCanvas.tsx`):**
    * Find all instances of `ctx.font = ...`
    * Update them to use Google Sans Flex. Example: `ctx.font = '10px "Google Sans Flex", sans-serif'`. Ensure text color is solid black (`#000000`) or dark gray (`#333333`) as seen in the mockup.

> **🛑 CHECKPOINT 1 FOR USER:**
> 1. Is every word and number in the UI now using Google Sans Flex?
> 2. Are the numbers in the transport and grid regular sans-serif numerals instead of stylized coding fonts?
> *(Agent: Wait for user confirmation before proceeding to Phase 2).*

---

### **PHASE 2: Crisp Line Thickness & Contrast**
**Objective:** Match the sharp, thin, high-contrast lines of the mockup for both HTML borders and Canvas grid lines.

1.  **HTML/Tailwind Borders (`App.tsx`):**
    * The horizontal lines separating the "Time Signature", "Bars and Beats", and "Audio" lanes must be crisp and visible.
    * Use `border-b border-black/20` or a very specific gray like `border-slate-400` on the containers to ensure the line is stark and not washed out.
2.  **Canvas Line Rendering (`RulerCanvas.tsx` & `SlicerCanvas.tsx`):**
    * **Crisp 1px Hack:** In HTML Canvas, drawing a 1px line on integer coordinates causes it to blur over 2 pixels. To get the razor-thin lines from the mockup, add `0.5` to your X and Y coordinates when drawing straight lines.
    * **Time Signature Lane:** Draw the vertical separator lines extending down from the flags. `ctx.lineWidth = 1`.
    * **Bars and Beats Lane:** Ensure the middle horizontal line and the vertical ticks are exactly 1px thick.
    * **Audio Lane (Grid):** The vertical grid lines extending behind the waveform must be perfectly thin. Use `ctx.lineWidth = 1` (or `0.5` depending on DPI scaling) and use a distinct dark gray for Tier 1 (Bars) and a medium/light gray for Tiers 2/3. 

> **🛑 CHECKPOINT 2 FOR USER:**
> 1. Are the horizontal dividers between the lanes distinct and sharp?
> 2. Are the vertical grid lines drawn on the canvas razor-thin and crisp?
> *(Agent: Wait for user confirmation before proceeding to Phase 3).*

---

### **PHASE 3: Lane Height Proportions**
**Objective:** Lock the heights of the Time Signature and Bars and Beats lanes to match the specific proportions of the mockup.

1.  **`App.tsx` (Lane Containers):**
    * Look at the left-hand label containers and their corresponding right-hand canvas containers.
    * **Time Signature Lane:** Set exactly to `h-[48px]`. Ensure the label text is vertically centered.
    * **Bars and Beats Lane:** Set exactly to `h-[48px]`. Ensure the label text is vertically centered.
    * **Audio Lane:** Set to `flex-1` so it expands to fill the remaining space, giving it the massive proportion seen in the mockup.
2.  **Canvas Heights:**
    * Ensure `TimeSigCanvas` is passed a height of exactly `48`.
    * Ensure `RulerCanvas` is passed a height of exactly `48`.
    * Update the internal Y-coordinate math in these canvases to accommodate the new 48px height (e.g., centering the middle line in the RulerCanvas at `height / 2`, which is now `24`).

> **🛑 FINAL CHECKPOINT FOR USER:**
> 1. Do the Time Signature and Bars & Beats lanes now have the correct vertical presence?
> 2. Does the Audio lane appropriately dwarf the upper lanes?