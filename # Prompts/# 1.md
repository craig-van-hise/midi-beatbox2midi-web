*

> **Role:** Expert React & Web Audio/Canvas Engineer
> 
> **Context:** Your previous implementation of the Transient Slicer is a visual shell. It is fundamentally broken and disconnected from the backend. You must fix the following critical bugs and missing features immediately.
> 
> **1. UI & Text Corrections:**
> * Remove the term "MVP" from the Title Bar and all user-facing text.
> * **Toolbar Expansion:** The toolbar must contain four distinct tools: Pointer, Pencil, Eraser, and **Lock** (using a padlock icon).
> 
> **2. Hit Point Aesthetics (Canvas Drawing):**
> * **Remove the circles:** Currently, hit points are drawn with a triangle and a circle. Stop this. They must be drawn ONLY as a vertical line topped with a downward-pointing solid triangle.
> * **Locked State Visual:** If a hit point is 'locked', draw a small padlock icon adjacent to or inside the top triangle.
> 
> **3. Audio Ingestion (Drag & Drop):**
> * The primary method for loading audio must be dragging and dropping a WAV/audio file directly onto the Main Workspace card. Implement `onDragOver` and `onDrop` event handlers that pass the file to the `AudioProcessor`.
> 
> **4. Waveform & WASM Disconnection (CRITICAL):**
> * **The Waveforms are fake/incorrect:** Extract the actual `Float32Array` from the decoded audio file and draw the true peaks/RMS on both the Minimap and the Main Canvas using the HTML5 Canvas API.
> * **Sliders do nothing:** Wire the `Sensitivity`, `Noise Floor`, and `Refractory` sliders directly to the `AudioProcessor` setter methods. Dragging these must trigger a debounced `calculateOnsets()`, retrieve the new array via `getTransients()`, and instantly trigger a canvas re-render.
> 
> **5. Minimap Resizing:**
> * The bounding box on the Waveform Overview must have interactive left and right handles. The user must be able to click and drag the edges to resize the viewport, not just move the rigid box.
> 
> **6. Canvas Interaction & Tool Logic:**
> * **Lock Tool:** Clicking a hit point with the Lock tool changes its state to 'locked' in the persistent state manager. It will now survive any changes made by the sensitivity sliders.
> * **Eraser Tool:** Clicking a hit point removes/mutes it. Change the CSS cursor to a custom 'X' or `crosshair` when active.
> * **Pencil Tool:** Track `onMouseMove` over the canvas and draw a faint/semi-transparent preview line where the hit point *will* be placed upon clicking. Click to add a locked hit point.
> * **Pointer Tool:** Clicking and dragging must draw a selection rectangle (marquee). Hit points caught inside become 'selected'. Pressing `Delete` or `Backspace` removes/mutes them.
> 
> **7. Tempo & Transport Fixes:**
> * **Tempo Input:** Must accept floating-point numbers up to 3 decimal places (e.g., `120.500`). Add a horizontal slider next to it for rapid sweeping.
> * **Playback & Playhead:** The Play button must trigger a Web Audio API `AudioBufferSourceNode` to play the decoded audio. Draw a moving vertical playhead line across the Main Canvas synchronized with `audioContext.currentTime`.
> * **Loop Locators:** Draw visual Left and Right locator markers. These must sit *below* the Bars/Beats ruler, positioned at the very top of the actual audio waveform space. Draw them as inward-facing flags (pointing towards the active loop region). The Web Audio playback must strictly loop between the times defined by these draggable locators.
> 
> **Task:** Do not generate placeholder functions. Write the actual Canvas rendering math and React state wiring to fix these specific areas.