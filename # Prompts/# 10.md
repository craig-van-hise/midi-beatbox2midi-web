
### **Master Engineering Prompt: Marquee, Overview Handles, & Loop Locators (CORRECTED)**

**Task: Implement Marquee batch processing, fix Overview viewport handles, and style Transport L/R Loop Locators.**

#### **1. Multi-Selection & Keyboard Shortcuts (`useHitManager.ts` & `SlicerCanvas.tsx`)**
* **State Upgrade:** Change `selectedHitIndex` to an array or Set (`selectedHitIndices`) to support multiple selections.
* **Key Listeners:** Attach a `keydown` event listener to the `window` (ensure it ignores events if the user is focused on an input).
    * **'M' / 'm' Key:** Iterate through `selectedHitIndices` and toggle their state to `muted` in the `hitStates` dictionary.
    * **'L' / 'l' Key:** Iterate through `selectedHitIndices` and toggle their state to `locked` in the `hitStates` dictionary.
* **Selection Rendering:** Ensure the Canvas renders *all* hitpoints present in `selectedHitIndices` as **Orange** ($#FF8C00$).

#### **2. Marquee Drag Logic (`SlicerCanvas.tsx`)**
* **Trigger:** When `activeTool === 'pointer'`, if the user triggers `onMouseDown` on empty canvas space (not hovering over a hitpoint), initialize a Marquee selection.
* **Drag State:** Track `marqueeStart {x, y}` and `marqueeCurrent {x, y}`.
* **Visuals:** On render, draw a semi-transparent selection rectangle (e.g., `fillStyle = 'rgba(100, 150, 255, 0.3)'` with a dashed stroke) matching the drag bounds.
* **Resolution (`onMouseUp`):** Calculate the start and end X-coordinates of the marquee box. Loop through the `renderableHits` array and add any hitpoint whose X-coordinate falls within these bounds to the `selectedHitIndices` array. Clear the marquee visual state.

#### **3. Overview Module: Zoom Window Handles (`WaveformMinimap.tsx`)**
* **Component Target:** This applies *strictly* to the blue viewport rectangle that controls zoom/scroll within the Minimap, NOT the playback loop locators.
* **Default Viewport:** Ensure the module initializes by displaying the entire audio file waveform from 0.0 to 1.0 duration.
* **Handle Affordance (Grips):** Redesign the left and right drag edges of this blue viewport window. Add a visual "grip" to these edges (e.g., a slightly wider vertical pill shape containing 3 small horizontal lines) so they clearly signal a "draggable" status.
* **Anti-Clipping:** Apply CSS `overflow: visible` to the parent containers (or adjust canvas rendering boundaries) so these new handle grips are never chopped off when pushed to the 0% or 100% edges of the minimap.

#### **4. Transport Playback Loop Locators (`RulerCanvas.tsx` / `SlicerCanvas.tsx`)**
* **Component Target:** This applies *strictly* to the loop locators used to define the audio playback range in the main workspace. 
* **Locator Geometry & Styling:** Redesign the Left and Right playback loop locators:
    * Draw the triangle heads substantially larger, hugging the very top edge of the ruler.
    * Draw "Flagpole" vertical lines extending from the triangle all the way to the bottom of the canvas.
    * Change their color to a high-visibility **Orange** ($#FF8C00$).
    * Explicitly draw the text **"L"** and **"R"** inside or immediately adjacent to their respective locator triangles.

