
### **Master Engineering Prompt: UX Interaction, Dual Hitboxes, & Audition Tool**

**Task: Refine SlicerCanvas interactions, update tool states, fix hitbox geometry, and implement the Audition playback engine.**

#### **1. Toolbar & Cursor State Updates**
* **Toolbar Icons:** Update the Eraser tool icon to a Lucide `X` icon. Add an Audition tool using the Lucide `Speaker` (or `Volume2`) icon.
* **Dynamic Cursors:** Modify the `SlicerCanvas` container style to use custom SVG data URIs for the cursor based on the `activeTool` state:
    * `mute` tool: Custom cursor resembling an 'X'.
    * `lock` tool: Custom cursor resembling a Padlock.
    * `audition` tool: Custom cursor resembling a Speaker.
    * `pointer` tool: Default arrow.

#### **2. Visual Rendering Refinements (`SlicerCanvas.tsx`)**
* **Locked State Overhaul:** When rendering a hitpoint with a `locked` state, **do not draw the triangle at all**. Instead, draw a larger padlock icon centered exactly over the timestamp line at the top of the canvas.
* **Hover Highlighting:** Implement an `onMouseMove` handler on the canvas that tracks a `hoveredHitIndex`.
    * *Render Logic:* If a hitpoint matches the `hoveredHitIndex`, render its vertical line thicker (e.g., `lineWidth = 2` or `3`) and slightly brighter to indicate affordance.

#### **3. Context-Aware Hitbox Math (`SlicerCanvas.tsx` Click/Hover Logic)**
Refactor the coordinate-matching math inside `onMouseMove` and `onMouseDown` to respect the `activeTool`:
* **If `activeTool === 'pointer'` (Triangle Strict Math):**
    * Only trigger if `Math.abs(mouseX - hitX) <= 5` (assuming a 10px wide triangle) **AND** `mouseY <= 15` (restrict to the top triangle area).
* **If `activeTool === 'mute'` OR `'lock'` (Line Proximity Math):**
    * Trigger if `Math.abs(mouseX - hitX) <= 8` (an 8-pixel buffer on either side of the vertical line), regardless of `mouseY`.

#### **4. The Audition Engine (`useAudioPlayback.ts` & Canvas Logic)**
* **Mode Activation:** When `activeTool === 'audition'`, disable hitpoint selection/manipulation.
* **Slice Identification:** When the user clicks on the canvas, calculate the exact time of the click based on the X coordinate.
    * Find the closest hitpoint *before* the click time (this is `sliceStart`).
    * Find the immediate next hitpoint *after* the click time (this is `sliceEnd`). If there is no next hitpoint, use the audio file's total duration.
* **Playback Trigger:** Add a `playSlice(startTime, endTime)` function to `useAudioPlayback.ts`. Calling this should immediately start playback at `sliceStart` and automatically stop it when the playhead reaches `sliceEnd`.

