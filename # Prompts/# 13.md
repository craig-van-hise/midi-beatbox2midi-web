
### **MASTER TDD EXECUTION PROMPT: ONBOARDING & TRANSPORT EXPANSION**

**Role & Instructions for Agent:**
You are an expert React, Web Audio API, and UI/UX engineer. Execute the following three phases strictly in order. After completing the modifications for a phase, pause, build the app, and wait for the user to visually verify the checkpoint before proceeding to the next phase.

---

### **PHASE 1: The "Empty State" Dropzone & Mic Record Logic**
**Objective:** Replace the empty application state with a dedicated drag-and-drop landing area and add the microphone recording pipeline.

1.  **`App.tsx` (State Additions):**
    * Import `Mic`, `FileAudio`, and `Radio` from `lucide-react`.
    * Add new states: 
        ```typescript
        const [isDraggingOver, setIsDraggingOver] = useState(false);
        const [isRecordingMic, setIsRecordingMic] = useState(false);
        const mediaRecorderRef = useRef<MediaRecorder | null>(null);
        const audioChunksRef = useRef<BlobPart[]>([]);
        ```
2.  **`App.tsx` (Microphone Recording Logic):**
    * Implement `startMicRecording` and `stopMicRecording` functions:
        ```typescript
        const startMicRecording = async () => {
          try {
            const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
            const mediaRecorder = new MediaRecorder(stream);
            mediaRecorderRef.current = mediaRecorder;
            audioChunksRef.current = [];

            mediaRecorder.ondataavailable = (e) => {
              if (e.data.size > 0) audioChunksRef.current.push(e.data);
            };

            mediaRecorder.onstop = () => {
              const audioBlob = new Blob(audioChunksRef.current, { type: 'audio/wav' });
              const file = new File([audioBlob], "recorded_audio.wav", { type: 'audio/wav' });
              loadFile(file);
              stream.getTracks().forEach(track => track.stop());
            };

            mediaRecorder.start();
            setIsRecordingMic(true);
          } catch (err) {
            console.error("Microphone access denied or failed", err);
          }
        };

        const stopMicRecording = () => {
          mediaRecorderRef.current?.stop();
          setIsRecordingMic(false);
        };
        ```
3.  **`App.tsx` (Drag Events & Conditional Rendering):**
    * Update the `<main>` element to handle drag states:
        ```tsx
        <main 
          className="flex-1 overflow-hidden relative"
          onDragOver={(e) => { e.preventDefault(); e.stopPropagation(); setIsDraggingOver(true); }}
          onDragLeave={(e) => { e.preventDefault(); e.stopPropagation(); setIsDraggingOver(false); }}
          onDrop={(e) => {
            e.preventDefault(); e.stopPropagation(); setIsDraggingOver(false);
            const file = e.dataTransfer.files?.[0];
            if (file && (file.type.startsWith('audio/') || file.name.endsWith('.wav'))) {
              loadFile(file);
            }
          }}
        >
        ```
    * Render the workspace *conditionally*: If `!audioBuffer`, render the Empty State UI instead of `Controls`, `WaveformMinimap`, and `SlicerCanvas`.
    * **Empty State UI:** A flex-centered div taking full height. If `isDraggingOver` is true, change the border to a glowing dashed blue and background to light blue. Include text: "Drag & Drop .WAV files here". Include a button calling `startMicRecording` (or `stopMicRecording` if currently recording, pulsing red).

> **🛑 CHECKPOINT 1 FOR USER:**
> 1. When the app loads, do you see the new Dashed Dropzone empty state instead of the waveform interface?
> 2. Does dragging a file over the window cause the area to visually highlight?
> 3. Does clicking the Record Mic button prompt for microphone access, pulse while recording, and instantly load the waveform when stopped?
> *(Agent: Wait for user confirmation before proceeding to Phase 2).*

---

### **PHASE 2: Transport UI Expansion (Time Sig, Metronome, Record, Tap Tempo)**
**Objective:** Flesh out the transport bar with DAW-standard structural controls.

1.  **`App.tsx` (New States & Props):**
    * Add `const [timeSignature, setTimeSignature] = useState<[number, number]>([4, 4]);`
    * Add `const [metronomeEnabled, setMetronomeEnabled] = useState(false);`
    * Pass these down to the `<Transport>` component.
2.  **`src/components/Transport.tsx` (Prop Updates & Tap Tempo Logic):**
    * Add `timeSignature`, `setTimeSignature`, `metronomeEnabled`, `setMetronomeEnabled`, `isRecording`, `toggleRecording` (pass the mic functions here too) to `TransportProps`.
    * Import `Bell`, `Hand`, `Circle` from `lucide-react`.
    * Implement Tap Tempo math:
        ```typescript
        const tapTimesRef = useRef<number[]>([]);
        const handleTapTempo = () => {
          const now = performance.now();
          tapTimesRef.current.push(now);
          if (tapTimesRef.current.length > 4) tapTimesRef.current.shift();
          
          if (tapTimesRef.current.length >= 2) {
            const deltas = [];
            for (let i = 1; i < tapTimesRef.current.length; i++) {
              deltas.push(tapTimesRef.current[i] - tapTimesRef.current[i - 1]);
            }
            const avgDelta = deltas.reduce((a, b) => a + b) / deltas.length;
            const newBpm = Math.round(60000 / avgDelta);
            if (newBpm >= 40 && newBpm <= 300) setTempo(newBpm);
          }
        };
        ```
3.  **`src/components/Transport.tsx` (UI Layout Adjustments):**
    * **Record Button:** Place a `Circle` icon button next to the Play button. Make it red if `isRecording` is true.
    * **Metronome Toggle:** Place a `Bell` icon button in the Playback Controls cluster. Toggle its color (blue/gray) based on `metronomeEnabled`.
    * **Time Signature:** Add to the left of the Tempo section. Create two small stacked `<input type="number">` fields (or side-by-side separated by `/`) bound to `timeSignature[0]` and `timeSignature[1]`.
    * **Tap Tempo:** Add a button labeled "TAP" with a `Hand` icon next to the Tempo slider. Bind `onClick={handleTapTempo}`.

> **🛑 CHECKPOINT 2 FOR USER:**
> 1. Are the new Time Signature inputs visible and editable?
> 2. Does clicking "TAP" repeatedly update the Tempo BPM correctly?
> 3. Are the Metronome (Bell) and Record (Circle) icons present in the transport cluster?
> *(Agent: Wait for user confirmation before proceeding to Phase 3).*

---

### **PHASE 3: Metronome Audio Implementation (Basic)**
**Objective:** Add an audible click track tied to the playhead and tempo.

1.  **`src/hooks/useAudioPlayback.ts` (Metronome Logic):**
    * Add `metronomeEnabled`, `tempo`, and `timeSignature` to the hook's arguments.
    * Inside the hook, we need a ref to track the last beat played: `const lastBeatRef = useRef(-1);`
    * Inside the `updatePlayhead` loop, calculate the current beat:
        ```typescript
        const beatsPerSecond = tempo / 60;
        const currentBeatExact = currentTime * beatsPerSecond;
        const currentBeatInt = Math.floor(currentBeatExact);

        if (metronomeEnabled && currentBeatInt > lastBeatRef.current) {
          lastBeatRef.current = currentBeatInt;
          
          // Synthesize a click
          if (audioCtxRef.current) {
            const osc = audioCtxRef.current.createOscillator();
            const gain = audioCtxRef.current.createGain();
            
            // High pitch on downbeat, lower pitch on other beats
            const isDownbeat = currentBeatInt % timeSignature[0] === 0;
            osc.frequency.value = isDownbeat ? 1200 : 800; 
            
            osc.connect(gain);
            gain.connect(audioCtxRef.current.destination);
            
            osc.start(audioCtxRef.current.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.001, audioCtxRef.current.currentTime + 0.1);
            osc.stop(audioCtxRef.current.currentTime + 0.1);
          }
        }
        
        // Reset lastBeatRef if we loop back
        if (isLooping && currentBeatInt < lastBeatRef.current) {
           lastBeatRef.current = -1;
        }
        ```
    * *Note for Agent:* Ensure `lastBeatRef` is reset to `-1` when `stop()` or `returnToZero()` is called so the click plays properly upon restarting.

> **🛑 FINAL CHECKPOINT FOR USER:**
> 1. When you toggle the Metronome on and hit Play, do you hear a synthesized beep?
> 2. Does the beep pitch change on the downbeat based on your Time Signature settings?